#!/bin/sh

if [ ! -d ./.archive ]; then
  mkdir .archive
fi

wget -qO- -O .archive/.tmp.zip https://www.stefanjudis.com/archive/index.zip && unzip .archive/.tmp.zip -d .archive && rm .archive/.tmp.zip
mv .archive/dist .archive/old-dist

