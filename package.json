{
  "name": "contentful-metalsmith-example",
  "version": "1.0.0",
  "description": "A example app that uses the contentful-metalsmith plugin",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/contentful-labs/contentful-metalsmith-example"
  },
  "scripts": {
    "dev": "MODE=PREVIEW npm start && MODE=PREVIEW run-p watch serve",
    "serve": "live-server ./build",
    "start": "node build.js",
    "lint": "standard \"helpers/**/*.js\" \"build.js\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "gazer --pattern '{src,layouts,helpers,partials}/**/*' node build.js"
  },
  "license": "MIT",
  "dependencies": {
    "contentful-metalsmith": "^0.5.0",
    "dateformat": "^1.0.12",
    "gazer": "^0.0.3",
    "glob": "^7.0.6",
    "handlebars": "^4.0.0",
    "highlight.js": "^9.7.0",
    "marked": "^0.3.6",
    "metalsmith": "^2.1.0",
    "metalsmith-assets": "^0.1.0",
    "metalsmith-clean": "^0.0.0",
    "metalsmith-copy": "^0.2.1",
    "metalsmith-data-markdown": "0.0.3",
    "metalsmith-drafts": "^0.0.1",
    "metalsmith-html-minifier": "^2.2.0",
    "metalsmith-layouts": "^1.6.5",
    "metalsmith-sass": "^1.3.0",
    "metalsmith-text-replace": "^1.0.1"
  },
  "devDependencies": {
    "cz-conventional-changelog": "^1.2.0",
    "live-server": "^1.1.0",
    "npm-run-all": "^3.0.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
